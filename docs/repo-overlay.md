# Moved: repo apply

The repo update/import command is documented at [repo-apply.md](repo-apply.md).

Use:

```bash
apex repo apply incoming.zip --repo . --create-missing --apply
```

This file remains only so old links do not dead-end.
